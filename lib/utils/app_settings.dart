import 'package:flutter/material.dart';


class AppSettings {
  /// Used to navigate within the authentication interceptor
  static GlobalKey<NavigatorState> navigatorState = GlobalKey<NavigatorState>();
}
